1、修改项目中的路径为相对路径，使项目在windows和linux服务器上运行时，项目结构一致。
2、模型训练优化器改用adamw。
3、优化imagenet参数，尽可能使用默认参数，除了imagenet数据集的路径、训练的批次大小等必要的参数外。
4、模型应该去重，搜索到相同的模型，应该不算做一次有效搜索
5、前期变异次数多，后期变异次数少，设计一个变异次数随时间衰减的机制。
6、记录每次变异和交叉的具体操作，方便后续分析。